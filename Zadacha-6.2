import java.util.Scanner;

public class Paroli {
    public static void main(String[] arguments){
        Scanner in = new Scanner(System.in);
        
        System.out.println("Enter N(1-9): ");
        int n = Integer.parseInt(in.nextLine());
        System.out.println("Enter L(1-9): ");
        int l = Integer.parseInt(in.nextLine());
        
        for(int i=1; i<=n; i++){
            for(int j=1; j<=n;j++){
                for(char l1='a'; l1<'a'+l; l1++){
                    for(char l2='a'; l2<'a'+l; l2++){
                        for(int s=Math.max(i, j)+1; s<=n; s++){
                            System.out.print(String.format("%d%d%c%c%d", 
                                    i, j, l1, l2, s));
                            System.out.print(" ");
                        }
                    }
                }
            }
        }
    }
}
